<div class="container">
  <h1>Mood</h1>
  <h4>Today you are feeling: <%= @average %></h4>


  <ul>
    <% @moods.each do |mood| %>
      <li>
      <%= mood.created_at.strftime("%D %H:%M %p %z") %>
      Mood: <%= mood.rating %> |
      Activity: <%= mood.activity_list %>
      </li>
    <% end %>
  <ul>

<%= link_to "Add mood", new_mood_path %>

</div>
